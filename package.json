{
  "name": "chaingang",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @chaingang/ep1 dev",
    "build": "pnpm -r build",
    "build:core": "pnpm --filter @chaingang/core build",
    "build:ep1": "pnpm --filter @chaingang/ep1 build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
